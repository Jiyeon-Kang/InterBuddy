import 'package:flutter/material.dart';

class ChatScreenStyles {
  const ChatScreenStyles._();

  static const EdgeInsets conversationPadding = EdgeInsets.all(16.0);
  static const EdgeInsets inputAreaPadding = EdgeInsets.all(8.0);
  static const SizedBox inputActionGap = SizedBox(width: 8);
  static const SizedBox messageGap = SizedBox(height: 12);
}
